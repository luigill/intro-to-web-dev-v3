const frase = "ThIs Is a PhRaSe";

// Builtin: Mudar o Caps de uma frase
const lowerCase = frase.toLowerCase();
const upperCase = frase.toUpperCase();

console.log(lowerCase);
console.log(upperCase);

// Cada tipo de dado possui métodos específicos que podem ser executados.
